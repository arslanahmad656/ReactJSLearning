import React from 'react'

const { Provider: DefatultContextProvider, Consumer: DefaultContextConsumer } = React.createContext('This is the default context');

export default {DefatultContextProvider, DefaultContextConsumer}